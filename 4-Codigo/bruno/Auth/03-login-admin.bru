meta { name: Login Administrador type: http seq: 3 }
post { url: {{baseUrl}}/api/auth/login body: json auth: none }
body:json { { "email": "admin@lumen.local", "password": "Lumen123!" } }
script:post-response {
  bru.setEnvVar("adminToken", res.getBody().accessToken);
}
