meta { name: Solicitar inscripción type: http seq: 1 }
post { url: {{baseUrl}}/api/enrollments body: json auth: bearer }
auth:bearer { token: {{studentToken}} }
body:json { { "courseId": "{{courseId}}" } }
